.container
  %ul
    - @board.errors.full_messages.each do |message|
      %li
        = message
  %h2.title New Board
  = form_with(model: @board, url: boards_path, method: 'post', local: true) do |f|
    %div
      = f.label :title, 'Name',class: 'first-label'
    %div
      = f.text_field :title, class:'text text-first'
    %div
      = f.label :content, 'Description',class: 'second-label'
    %div
      = f.text_area :content, class: 'text text-a'
    = f.submit '投稿', class: 'btn' 
   
 